version: '3.3'

services:
  mongodb:
    image: mongo:3.6.5
    volumes:
      - mongodb_app:/data/db
    networks:
      - app-network

volumes:
  mongodb_app:

networks:
  app-network:
    external:
      name: ${DOCKER_NETWORK}