version: '3.3'

services:
  app:
    environment:
      - DEBUG=
      - DB_URL=mongodb://mongodb:27017/${APP}
    working_dir: /opt/kdk/${APP}
    depends_on:
      - mongodb
    volumes:
      - ../coverage:/opt/kdk/${APP}/api/coverage
    command: [ "npm", "run", "test:server" ]
